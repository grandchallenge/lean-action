name: GHOS hostile unregistered inert probe

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inert:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Inert sentinel
        run: echo "This hostile-proof job must never execute."
