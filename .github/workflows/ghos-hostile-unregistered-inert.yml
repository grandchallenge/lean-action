name: GHOS hostile unregistered inert probe

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inert:
    if: ${{ github.repository == 'grandchallenge/__ghos_never__' }}
    runs-on: ubuntu-latest
    steps:
      - name: Inert sentinel
        run: echo "This hostile-proof job must never execute."
